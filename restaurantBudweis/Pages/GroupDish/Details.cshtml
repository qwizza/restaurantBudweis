@page
@model restaurantBudweis.Pages.GroupDishs.DetailsModel
@{
    ViewData["Title"] = "Информация о группе блюд";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card shadow-sm">
                <div class="card-header bg-primary text-white">
                    <h4 class="mb-0">Информация о группе блюд</h4>
                </div>
                <div class="card-body">
                    <dl class="row">
                        <dt class="col-sm-3">Название</dt>
                        <dd class="col-sm-9">@Model.GroupDish.Name</dd> 
                        @if (!string.IsNullOrEmpty(Model.GroupDish.Dishs))
                        {
                            <dt class="col-sm-3">Описание</dt>
                            <dd class="col-sm-9">@Model.GroupDish.Dishs</dd>

                        }
                        else
                        {
                            <dt class="col-sm-3">Описание</dt>
                            <dd class="col-sm-9 text-muted fst-italic">Описание не указано</dd>
                        }
                    </dl>

                    <div class="d-flex justify-content-between mt-3">
                        <a asp-page="Index" class="btn btn-secondary">Назад к списку</a>
                        <div>
                            <a asp-page="Edit" asp-route-id="@Model.GroupDish.Id" class="btn btn-primary">Редактировать</a>
                            <a asp-page="Delete" asp-route-id="@Model.GroupDish.Id" class="btn btn-danger">Удалить</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>